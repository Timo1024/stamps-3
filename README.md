# start

docker-compose build

docker-compose up -d

# stop

docker-compose down

# check if is running

docker ps -a
docker-compose ps
docker logs mysql_container

# check backend

http://localhost:8000/docs